# 贡献说明

感谢你关注这个项目。提交改动时请尽量保持实现简单、范围清晰，并优先保证浏览器端的本地可用性。

## 开发流程

1. Fork 或克隆仓库。
2. 基于 `main` 创建分支。
3. 修改 `index.html` 或文档文件。
4. 使用浏览器打开 `index.html` 进行手动验证。
5. 提交 Pull Request，并说明改动内容与验证方式。

## 代码风格

- 保持单文件应用结构，除非有明确理由拆分。
- 不引入构建工具或第三方依赖，除非能显著改善核心体验。
- 优先使用浏览器原生 API。
- 文案使用中文，命名保持清晰直接。
- 不提交测试视频、导出图片或其他大体积媒体文件。

## 手动验证建议

- 导入一个浏览器可播放的视频。
- 测试按张数、按秒数和手动切帧。
- 测试片段范围截帧。
- 测试拼图导出和单图导出。
- 调整窗口宽度，确认主要布局没有明显遮挡或溢出。
